// Define the button element
const addStoresBtn = document.querySelector('#add-stores-btn');

// Define a function to execute when the button is clicked
function addStores() {
  // Code to add stores goes here
  console.log('Stores added!');
}

// Attach the function to the button's click event
addStoresBtn.addEventListener('click', addStores);
